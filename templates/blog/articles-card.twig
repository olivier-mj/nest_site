{# <article class="p-3 card-post lg:w-1/3 md:w-1/2">
    {% if post.filename %}
    <div class="h-full overflow-hidden rounded-lg shadow-md ">
        <img height="192px" width="286px" src="{{ vich_uploader_asset(post) |
                imagine_filter('blog_thumbnail') }}" class="object-cover object-center w-full lg:h-48 md:h-36 lazyload"
            alt="{{ post.title }}">
        <span class="gradient"></span>
    </div>

    {% endif %}
    <div class="details">
        <div class="category" style="color: {{ post.category.color }} !important;">
            <b>{{ post.category.name }}</b>
        </div>
        <h3><a href="{{ path('blog.show', {id: post.id, slug: post.slug }) }}">{{ post.title }}</a></h3>
        <small><time class="text-white timeago small" datetime="{{ post.createdAt| date('c')
                }}">{{ post.createdAt|
                    format_datetime('medium', 'short', locale='fr') }}</time></small>
    </div>
</article> #}

<div class="p-3 lg:w-1/4 md:w-1/2">
    <a href="{{ path('blog.show', {'slug': post.slug, 'id': post.id}) }}">
        <!--Card 1-->
        <div class="max-w-sm overflow-hidden rounded shadow-lg ">
            <img class="w-full lazyload" src="{{ vich_uploader_asset(post) | imagine_filter('blog_thumbnail') }}"
                alt="{{ post.title }}" width="384px" height="216px">
            <div class="px-4 py-4">
                <div class="mb-2 text-xl font-bold">{{ post.title }}</div>
                <time class="mb-2 text-gray-700 timeago " datetime="{{ post.createdAt| date('c')
                }}">{{ post.createdAt | format_datetime('medium', 'short', locale='fr') }}</time>
                <p class="text-base text-gray-700">
                    {{ post.content | raw | striptags| slice(0,70) }} ...
                </p>
            </div>
            <div class="px-4 pt-4 pb-2">
                {% for tag in post.tags %}
                <span
                    class="inline-block px-3 py-1 mb-2 mr-2 text-sm font-semibold text-gray-700 bg-gray-200 rounded-full">
                    #{{ tag.name}}
                </span>
                {% endfor %}


            </div>
        </div>
    </a>
</div>